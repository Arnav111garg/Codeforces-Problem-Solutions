#include <bits/stdc++.h>
using namespace std;

void solve() {
    int m, n;
    if (!(cin >> m >> n)) return;

    cout << (m * n) / 2 << "\n";
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 1;
    // cin >> t;
    while (t--) {
        solve();
    }

    return 0;
}