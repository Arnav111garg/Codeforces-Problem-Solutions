#include <bits/stdc++.h>
using namespace std;

void solve() {
    long long n;
    if (!(cin >> n)) return;


    cout << n / 2520 << "\n";
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 1; 
    while (t--) {
        solve();
    }

    return 0;
}