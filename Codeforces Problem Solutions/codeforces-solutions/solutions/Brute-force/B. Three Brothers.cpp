#include <bits/stdc++.h>
using namespace std;

void solve() {
    int a, b;
    if (cin >> a >> b) {
        cout << 6 - (a + b) << "\n";
    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 1;
    while (t--) {
        solve();
    }

    return 0;
}